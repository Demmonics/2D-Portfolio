import { k } from "./kaboomCtx.js";
k.loadSprite("spritesheet", "./sprite.png", kabooom inspector) // to inspect the game in the browser);
k.loadSprite("spritesheet", "./spritesheet.png"{// in vite we dont have to add other things like public folder becuase vite considers abything in the piblic folder easily accesible hencofrth makes the program easier to read
     slicex 39;// count the number of frames in the spritesheet then divide the number by 16 to get the x value
     slicey 33, // count the number of frames in the spritesheet then divide the number by 16 to get the y value 

})
